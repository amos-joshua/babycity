name: babycity
description: "A new Flutter project."
publish_to: 'none'

version: 0.1.0

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  #flutter_osm_plugin: ^1.0.2
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  flutter_map_cancellable_tile_provider: ^2.0.0
  url_launcher: ^6.2.6
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
